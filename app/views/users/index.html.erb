<h1>Total Users <%=@users.size%> </h1>
<%@users.each do |user|%>
<li>
<%= link_to user.name, "/users/#{user.id}"%> created <%= time_ago_in_words(user.created_at)%> ago
</li>
<%end%>
<%= link_to "Add New Account", "/users/new"%>
